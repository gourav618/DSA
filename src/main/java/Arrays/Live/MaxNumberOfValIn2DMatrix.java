package Arrays.Live;

public class MaxNumberOfValIn2DMatrix {
    public static void main(String [] args){
        //given a 2D matrix initially all vals are 0
        //queries (x, y) -> (3,2) == from (0,0) to (3,2) increment by 1
        //                  (2, 6) == from (0,0) to (2,6) increment by 1

        //tell max number of cells containing maximum val inside them

        // here we need to find the min val of x and y
        //since we need to find overlapping area which experience overlapping increment

        //if some question is tricky think of lower dimension i.e in this case 1d array
        //
    }
}
